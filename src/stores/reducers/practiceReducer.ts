import { createSlice } from "@reduxjs/toolkit";

const practiceReducer = createSlice({
    name: "practice",
    initialState: {},
    reducers: {},
});

export const {} = practiceReducer.actions;

export default practiceReducer.reducer;
