import { createSlice } from "@reduxjs/toolkit";

const displaySlide = createSlice({
    name: "display",
    initialState: {},
    reducers: {},
});

export const {} = displaySlide.actions;

export default displaySlide.reducer;
