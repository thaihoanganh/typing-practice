import { createSlice } from "@reduxjs/toolkit";

const lessonsReducer = createSlice({
    name: "lessons",
    initialState: {},
    reducers: {},
});

export const {} = lessonsReducer.actions;

export default lessonsReducer.reducer;
