import { createSlice } from "@reduxjs/toolkit";

const statisticalReducer = createSlice({
    name: "statistical",
    initialState: {},
    reducers: {},
});

export const {} = statisticalReducer.actions;

export default statisticalReducer.reducer;
